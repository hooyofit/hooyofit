name: Keep Supabase Alive

on:
  schedule:
    - cron: '0 9 */3 * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase project
        run: |
          curl -s -o /dev/null -w "%{http_code}" "https://qmiutlpdhmuqfymruxal.supabase.co/rest/v1/profiles?select=id&limit=1" -H "apikey: sb_publishable_MqQ9YRxbxltWr8hoVcrcgQ_yC7UxeEX"
